{
  "name": "ksp-crime-intel-datathon",
  "version": "1.0.0",
  "private": true,
  "description": "KSP Crime Intelligence Platform deployment configuration for Zoho Catalyst Slate",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --prefix client"
  }
}
